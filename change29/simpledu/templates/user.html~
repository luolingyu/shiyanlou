{% extends 'base.html' %}
{% block title %}Home{% endblock %}

{% block body %}
   {% for user in users %}
   <h1>ID:{{ user.id }}</h1>
   <h1>Username:{{ user.username }}</h1>
   <h1>Published courses:</h1>
   {% for courses in user.published_courses %}
   <p> {{ published_courses }}</p>
    {% endfor %}
   {% endfor %}
{% endblock %}
